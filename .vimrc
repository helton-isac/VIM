" Execute the plugin manager
execute pathogen#infect()

" Esquema de cores escuto
" colorscheme industry
colorscheme Tomorrow-Night-Eighties

" Tamanho da Identação
set tabstop=2

" Identifica o tipo de arquivo e identa
filetype plugin indent on

" Colorir o editor
syntax on

" Deixar coerente identação com tamamnho de TAB
set shiftwidth=2

" comportamento usual do backspace
set backspace=2

" This command is used to number the lines
set number

" Fazer calculo da distância das linhas
set relativenumber

" Busca incremental feedback enquanto busco
set incsearch

" destaque nos resultados
set hlsearch

" Salvar na codificação desejada
"set fileencoding=iso-8859-1

" Visualizar na codificação desejada
"set encoding=iso-8859-1 

" Usar espaÃ§os no lugar de TAB
set expandtab

" Backspace respeitar identaÃ§Ã£o
set softtabstop=2

